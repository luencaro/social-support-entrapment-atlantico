.Rproj.user
.Rhistory
.RData
.Ruserdata

# datos de participantes: no se versionan (datos sensibles de menores,
# celdas pequenas por municipio -> riesgo de reidentificacion).
# La carpeta data/ si se versiona (via .gitkeep), solo su contenido no.
/data/*
!/data/.gitkeep

# ruta real del archivo de datos (nombre del archivo incluido): local a cada quien
.Renviron

# bookdown build artifacts (docs/ is committed for GitHub Pages)
/_bookdown_files/
/eda-apoyo-social-entrapment_files/
*.knit.md
*.utf8.md
_main.Rmd
